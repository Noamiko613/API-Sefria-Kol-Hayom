
services:
  sefria-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sefria-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data:ro
    environment:
      - NODE_ENV=production
      - PORT=3000
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - sefria-network

  # EXPOSE YOUR API WORLDWIDE WITH ZROK (STABLE TUNNEL)
  zrok:
    image: openziti/zrok
    container_name: sefria-zrok
    # We join the API's network namespace so 'localhost:3000' routes directly to the API container
    network_mode: "service:sefria-api"
    environment:
      - HOME=/tmp/zrok
    user: root
    entrypoint: sh
    command: -c "zrok enable 0hD9JBznYqUN && zrok share public http://localhost:3000 --headless"
    restart: unless-stopped
    depends_on:
      - sefria-api

networks:
  sefria-network:
    driver: bridge
